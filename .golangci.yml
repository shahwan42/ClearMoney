version: "2"

linters:
  default: none
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
  exclusions:
    rules:
      # Allow unused parameters in test files
      - path: _test\.go
        linters:
          - unused
