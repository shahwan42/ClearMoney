"""
Admin configuration — intentionally empty.

All models use managed=False (Go owns the schema), and Django admin is not
installed. This file exists to satisfy Django's app structure requirements.
"""
