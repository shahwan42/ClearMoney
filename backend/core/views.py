"""
Core views — intentionally empty.

The core app provides shared infrastructure (models, middleware, template tags)
but no HTTP views. All routes live in their respective apps (dashboard, accounts, etc.).
"""
